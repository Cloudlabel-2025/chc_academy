"use client";
import {useEffect,useState} from "react";
import "./voice-review.css";
type Trainee={email:string;fullName:string;cohort:string};type Session={id:string;area:string;years:number;score:number;answersJson:string;createdAt:string};type Audio={id:string;sessionId:string;questionIndex:number;sizeBytes:number;createdAt:string};
export default function VoiceReview(){const[trainees,setTrainees]=useState<Trainee[]>([]),[email,setEmail]=useState(""),[sessions,setSessions]=useState<Session[]>([]),[audio,setAudio]=useState<Audio[]>([]);useEffect(()=>{fetch("/api/trainer").then(r=>r.json()).then(d=>{setTrainees(d.trainees||[]);if(d.trainees?.[0])select(d.trainees[0].email)})},[]);async function select(x:string){setEmail(x);const[s,a]=await Promise.all([fetch(`/api/interviews?email=${encodeURIComponent(x)}`).then(r=>r.json()),fetch(`/api/interview-audio?email=${encodeURIComponent(x)}`).then(r=>r.json())]);setSessions(s.sessions||[]);setAudio(a.audio||[])}return <main className="voice-review-shell"><header><a href="/">CHC · ORACLE HCM <span>VOICE INTERVIEW REVIEW</span></a><nav><a href="/trainer">Trainer console</a><a href="/">Academy</a></nav></header><section className="voice-review-head"><div><small>TRAINER REVIEW</small><h1>Listen to interview answers</h1><p>Voice responses remain attached to the original question and interview attempt.</p></div><label>Trainee<select value={email} onChange={e=>select(e.target.value)}><option value="">Select trainee</option>{trainees.map(t=><option value={t.email} key={t.email}>{t.fullName} · {t.cohort}</option>)}</select></label></section><section className="voice-sessions">{sessions.map(s=>{const answers=JSON.parse(s.answersJson||"[]") as {q:string;a:string;score:number;audioId?:string}[];return <article key={s.id}><div className="session-head"><div><small>{new Date(s.createdAt).toLocaleDateString()} · {s.years} YEARS</small><h2>{s.area}</h2></div><b>{answers.some(a=>a.audioId)?"VOICE REVIEW":"TEXT SCORE"}<span>{s.score}%</span></b></div>{answers.map((a,i)=>{const recording=audio.find(x=>x.sessionId===s.id&&x.questionIndex===i);return <div className="voice-question" key={a.q}><span>{i+1}</span><div><b>{a.q}</b>{recording?<audio controls preload="none" src={`/api/interview-audio?id=${recording.id}`}/>:<p>{a.a||"No answer stored."}</p>}</div><em>{recording?"VOICE":`${a.score}/10`}</em></div>})}</article>})}{email&&!sessions.length&&<div className="voice-empty">No mock interview attempts for this trainee.</div>}{!email&&<div className="voice-empty">Select a trainee to review their attempts.</div>}</section></main>}
